---
import ToolsLayout from '../../../layouts/ToolsLayout.astro';
import MacroCalculatorTool from '../../../components/tools/MacroCalculatorTool';
import { formatSeoTitle } from '@shared/config/seo';
import { utilitiesHref } from '@shared/config/sites';

const toolName = 'Macro & Calorie Calculator';
const toolDescription =
  'Calculate daily calories and macro split (protein, fat, carbs) using the Mifflin-St Jeor equation — fully offline.';
---

<ToolsLayout
  title={formatSeoTitle(toolName, 'Free Online Tool')}
  description={toolDescription}
  activeNav="life"
>
  <div>
    <header class="mb-6">
      <a href={utilitiesHref('/tools/life')} class="text-xs font-semibold text-emerald-400 transition hover:text-emerald-300">
        ← Life &amp; Focus Tracker
      </a>
      <h1 class="mt-2 text-2xl font-bold text-white sm:text-3xl">{toolName}</h1>
      <p class="mt-1 text-sm text-slate-400">{toolDescription}</p>
    </header>
    <MacroCalculatorTool client:load />
  </div>
</ToolsLayout>
