---
import ToolsLayout from '../../../layouts/ToolsLayout.astro';
import SipCalculatorTool from '../../../components/tools/SipCalculatorTool';
import { formatSeoTitle } from '@shared/config/seo';
import { utilitiesHref } from '@shared/config/sites';

const toolName = 'SIP Calculator';
const toolDescription =
  'Estimate mutual fund SIP maturity with step-up contributions and inflation adjustment — free and 100% offline.';
---

<ToolsLayout
  title={formatSeoTitle(toolName, 'Free Online Tool')}
  description={toolDescription}
  activeNav="money"
>
  <div>
    <header class="mb-6">
      <a href={utilitiesHref('/tools/money')} class="text-xs font-semibold text-emerald-700 transition hover:text-emerald-800">
        ← Money &amp; Gig Ledger
      </a>
      <h1 class="mt-2 text-2xl font-bold text-slate-900 sm:text-3xl">{toolName}</h1>
      <p class="mt-1 text-sm text-slate-600">
        Premium investment growth forecaster with step-up and real-value inflation adjustment — fully offline.
      </p>
    </header>
    <SipCalculatorTool client:load />
  </div>
</ToolsLayout>
