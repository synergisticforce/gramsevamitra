---
import ToolsLayout from '../../../layouts/ToolsLayout.astro';
import OcrExtractor from '../../../components/tools/OcrExtractor';
import { formatSeoTitle } from '@shared/config/seo';
import { utilitiesHref } from '@shared/config/sites';

const toolName = 'Browser OCR Text Extractor';
const toolDescription =
  'Extract text from images using ONNX WASM (TrOCR via Transformers.js) — grayscale, contrast, and binarization preprocessing, Web Worker inference, 100% offline.';
---

<ToolsLayout
  title={formatSeoTitle(toolName, 'Free Online Tool')}
  description={toolDescription}
  activeNav="writing"
>
  <div class="ocr-extractor-page">
    <header class="mb-6">
      <a href={utilitiesHref('/tools/writing')} class="text-xs font-semibold text-emerald-400 transition hover:text-emerald-300">
        ← Smart Writing Studio
      </a>
      <h1 class="mt-2 text-2xl font-bold text-white sm:text-3xl">{toolName}</h1>
      <p class="mt-1 text-sm text-slate-400">
        Next-gen client-side OCR — no Tesseract, no server. Model downloads once and caches in your browser.
      </p>
    </header>

    <OcrExtractor client:load />
  </div>
</ToolsLayout>
