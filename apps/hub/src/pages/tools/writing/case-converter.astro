---
import ToolsLayout from '../../../layouts/ToolsLayout.astro';
import CaseConverterTool from '../../../components/tools/CaseConverterTool';
import ToolBackLink from '../../../components/tools/ToolBackLink.astro';
import { formatSeoTitle } from '@shared/config/seo';
import { utilitiesHref } from '@shared/config/sites';

const toolName = 'Case Converter';
const toolDescription =
  'Convert text to UPPERCASE, lowercase, Title Case, Sentence case, or alternating — 100% offline.';
---

<ToolsLayout title={formatSeoTitle(toolName, 'Free Online Tool')} description={toolDescription} activeNav="writing">
  <div>
    <header class="mb-6">
      <ToolBackLink href={utilitiesHref('/tools/writing')} label="← Smart Writing Studio" />
      <h1 class="mt-2 text-2xl font-bold text-slate-900 sm:text-3xl">{toolName}</h1>
      <p class="mt-1 text-sm text-slate-600">{toolDescription}</p>
    </header>
    <CaseConverterTool client:load />
  </div>
</ToolsLayout>
