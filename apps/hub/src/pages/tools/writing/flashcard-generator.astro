---
import ToolsLayout from '../../../layouts/ToolsLayout.astro';
import FlashcardGeneratorTool from '../../../components/tools/FlashcardGeneratorTool';
import { formatSeoTitle } from '@shared/config/seo';
import { utilitiesHref } from '@shared/config/sites';

const toolName = 'Flashcard Generator';
const toolDescription =
  'Paste notes as Question-Answer pairs and study with interactive flip cards — no account required.';
---

<ToolsLayout
  title={formatSeoTitle(toolName, 'Free Online Tool')}
  description={toolDescription}
  activeNav="writing"
>
  <div>
    <header class="mb-6">
      <a href={utilitiesHref('/tools/writing')} class="text-xs font-semibold text-emerald-700 transition hover:text-emerald-800">
        ← Smart Writing Studio
      </a>
      <h1 class="mt-2 text-2xl font-bold text-slate-900 sm:text-3xl">{toolName}</h1>
      <p class="mt-1 text-sm text-slate-600">{toolDescription}</p>
    </header>
    <FlashcardGeneratorTool client:load />
  </div>
</ToolsLayout>
