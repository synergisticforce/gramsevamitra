---
import ToolsLayout from '../../../layouts/ToolsLayout.astro';
import SalaryBenchmarker from '../../../components/tools/SalaryBenchmarker';
import { formatSeoTitle } from '@shared/config/seo';
import { utilitiesHref } from '@shared/config/sites';

const toolName = 'Salary Benchmarking';
const toolDescription =
  'Offline salary ranges by role and experience band — search roles and compare min/mid/max CTC in lakhs.';
---

<ToolsLayout
  title={formatSeoTitle(toolName, 'Free Online Tool')}
  description={toolDescription}
  activeNav="career"
>
  <div class="salary-benchmark">
    <header class="mb-6">
      <a href={utilitiesHref('/tools/career')} class="text-xs font-semibold text-emerald-400 transition hover:text-emerald-300">
        ← Career Prep Room
      </a>
      <h1 class="mt-2 text-2xl font-bold text-white sm:text-3xl">{toolName}</h1>
      <p class="mt-1 text-sm text-slate-400">
        Indicative annual CTC ranges (₹ lakhs) — filters compile dynamically from loaded benchmark data.
      </p>
    </header>

    <SalaryBenchmarker client:load />
  </div>
</ToolsLayout>
