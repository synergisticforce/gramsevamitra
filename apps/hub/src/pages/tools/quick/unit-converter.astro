---
import ToolsLayout from '../../../layouts/ToolsLayout.astro';
import UnitConverterTool from '../../../components/tools/UnitConverterTool';
import ToolBackLink from '../../../components/tools/ToolBackLink.astro';
import { formatSeoTitle } from '@shared/config/seo';
import { utilitiesHref } from '@shared/config/sites';

const toolName = 'Unit Converter';
const toolDescription =
  'Convert length, weight, and temperature instantly — bidirectional, real-time, and 100% offline.';
---

<ToolsLayout title={formatSeoTitle(toolName, 'Free Online Tool')} description={toolDescription} activeNav="quick">
  <div>
    <header class="mb-6">
      <ToolBackLink href={utilitiesHref('/tools/quick')} label="← Quick Tools Drawer" />
      <h1 class="mt-2 text-2xl font-bold text-white sm:text-3xl">{toolName}</h1>
      <p class="mt-1 text-sm text-slate-400">{toolDescription}</p>
    </header>
    <UnitConverterTool client:load />
  </div>
</ToolsLayout>
