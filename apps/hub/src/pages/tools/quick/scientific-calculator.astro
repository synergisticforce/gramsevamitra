---
import ToolsLayout from '../../../layouts/ToolsLayout.astro';
import ScientificCalculatorTool from '../../../components/tools/ScientificCalculatorTool';
import { formatSeoTitle } from '@shared/config/seo';
import { utilitiesHref } from '@shared/config/sites';

const toolName = 'Scientific Calculator';
const toolDescription =
  'Scientific calculator with trig, log, powers, and calculation history — runs entirely in your browser.';
---

<ToolsLayout
  title={formatSeoTitle(toolName, 'Free Online Tool')}
  description={toolDescription}
  activeNav="quick"
>
  <div>
    <header class="mb-6">
      <a href={utilitiesHref('/tools/quick')} class="text-xs font-semibold text-emerald-400 transition hover:text-emerald-300">
        ← Quick Tools Drawer
      </a>
      <h1 class="mt-2 text-2xl font-bold text-white sm:text-3xl">{toolName}</h1>
      <p class="mt-1 text-sm text-slate-400">{toolDescription}</p>
    </header>
    <ScientificCalculatorTool client:load />
  </div>
</ToolsLayout>
