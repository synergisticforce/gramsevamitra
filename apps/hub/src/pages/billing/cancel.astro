---
import AppShellLayout from '../../layouts/AppShellLayout.astro';
import { formatSeoTitle } from '@shared/config/seo';

const pageTitle = formatSeoTitle('Checkout Cancelled', 'GramSeva Mitra');
---

<AppShellLayout title={pageTitle} description="Your Pro upgrade checkout was cancelled — no charge was made." activeWorkspace="documents">
  <div class="px-4 py-12 text-center sm:px-6 sm:py-16">
    <h1 class="text-2xl font-bold text-canvas-text sm:text-3xl">Checkout cancelled</h1>
    <p class="mx-auto mt-3 max-w-md text-sm leading-relaxed text-canvas-muted">
      No worries — you were not charged. Free workspaces remain fully available anytime.
    </p>
    <a
      href="/workspace/documents"
      class="mt-8 inline-flex items-center justify-center rounded-lg bg-canvas-accent-muted px-4 py-2.5 text-sm font-semibold text-canvas-text transition hover:bg-canvas-elevated"
    >
      Continue with free tools
    </a>
  </div>
</AppShellLayout>
