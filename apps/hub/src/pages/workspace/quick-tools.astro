---
import AppShellLayout from '../../layouts/AppShellLayout.astro';
import QuickToolsHubCanvas from '../../components/canvas/QuickToolsHubCanvas';
import { APP_WORKSPACES } from '../../config/appWorkspaces';
import { formatSeoTitle } from '@shared/config/seo';

const workspace = APP_WORKSPACES.find((ws) => ws.id === 'quick-tools')!;
---

<AppShellLayout
  title={formatSeoTitle(workspace.label, 'GramSeva Mitra App')}
  description={workspace.description}
  activeWorkspace="quick-tools"
>
  <QuickToolsHubCanvas client:load />
</AppShellLayout>
