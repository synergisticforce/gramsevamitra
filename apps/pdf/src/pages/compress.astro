---
import '@shared/styles/global.css';
import BaseLayout from '@shared/layouts/BaseLayout.astro';
import Header from '@shared/components/Header.astro';
import Footer from '@shared/components/Footer.astro';
import CompressPage from '../components/CompressPage';
import { formatSeoTitle } from '@shared/config/seo';

const toolName = 'Compress PDF';
const toolDescription =
  'Shrink PDF file size for exam uploads — extreme, balanced, and high-quality presets. 100% private, runs in your browser.';
---

<BaseLayout
  title={formatSeoTitle('Compress PDF Size', 'Free Online Tool')}
  description={toolDescription}
  site="pdf"
>
  <Header slot="header" active="pdf" />

  <section class="relative overflow-hidden">
    <div
      class="pointer-events-none absolute inset-0 bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-emerald-900/20 via-slate-950 to-slate-950"
    >
    </div>

    <div class="relative mx-auto max-w-3xl px-4 py-8 sm:px-6 sm:py-12">
      <header class="mb-6">
        <a href="/" data-astro-reload class="text-xs font-semibold text-emerald-400 transition hover:text-emerald-300">
          ← PDF Tools Home
        </a>
        <h1 class="mt-3 text-2xl font-bold text-white sm:text-3xl">{toolName}</h1>
        <p class="mt-1 text-sm text-slate-400">{toolDescription}</p>
      </header>

      <div class="relative overflow-visible rounded-2xl border border-emerald-800/50 bg-slate-900/80 p-4 sm:p-6">
        <CompressPage client:load />
      </div>

      <p class="mt-6 text-center text-xs text-slate-500">
        Need more tools?
        <a href="/merge" class="font-semibold text-emerald-400 hover:text-emerald-300">Merge PDF</a>
        ·
        <a href="/studio" class="font-semibold text-emerald-400 hover:text-emerald-300">Full PDF Studio</a>
      </p>
    </div>
  </section>

  <Footer slot="footer" />
</BaseLayout>
