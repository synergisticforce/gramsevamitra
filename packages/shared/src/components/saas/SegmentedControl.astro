---
export interface SegmentItem {
  id: string;
  label: string;
  href: string;
}

interface Props {
  items: SegmentItem[];
  activeId: string;
  ariaLabel?: string;
}

const { items, activeId, ariaLabel = 'Section navigation' } = Astro.props;
---

<nav
  class="inline-flex w-full max-w-md rounded-lg bg-canvas-elevated p-1"
  aria-label={ariaLabel}
>
  {
    items.map((item) => {
      const isActive = item.id === activeId;
      return (
        <a
          href={item.href}
          class:list={[
            'flex-1 rounded-md px-3 py-2 text-center text-xs font-semibold transition sm:text-sm',
            isActive
              ? 'bg-canvas-surface text-canvas-text shadow-none'
              : 'text-canvas-muted hover:text-canvas-text',
          ]}
          aria-current={isActive ? 'page' : undefined}
        >
          {item.label}
        </a>
      );
    })
  }
</nav>
