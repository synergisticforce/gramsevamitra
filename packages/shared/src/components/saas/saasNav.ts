/** Platform shell navigation keys — shared across hub and PDF apps. */
export type SaaSSiteId = 'hub' | 'pdf';

export type SaaSNavKey = 'home' | 'utilities' | 'contact' | 'legal' | 'studio';

export const OMNI_SEARCH_OPEN_EVENT = 'gsm-omni-search-open';
